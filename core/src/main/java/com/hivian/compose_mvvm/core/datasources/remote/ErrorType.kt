package com.hivian.compose_mvvm.core.datasources.remote

enum class ErrorType {
    ACCESS_DENIED,
    CANCELLED,
    HOST_UNREACHABLE,
    TIMED_OUT,
    NO_RESULT,
    DATABASE_ERROR,
    UNKNOWN,
}
